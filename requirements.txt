# Streamlit deployment requirements
streamlit>=1.28.0

# Core PyTorch and ML dependencies - Updated for Python 3.10+ compatibility
torch>=2.0.0
torchaudio>=2.0.0
transformers==4.44.1
einops==0.8.0
accelerate>=0.24.0

# SongBloom core dependencies
huggingface-hub==0.24.6
jieba-fast==0.53
pypinyin==0.51.0
cn2an==0.5.22
wordsegment==1.3.1
g2p-en==2.1.0
lightning==2.2.1
nltk==3.8.1
omegaconf==2.2.0
spacy==3.7.4
num2words==0.5.13

# Audio codec - Skip for now to resolve protobuf conflict
# descript-audio-codec will be loaded dynamically if available
# descript-audio-codec==1.0.0
vector_quantize_pytorch==1.14.8

# Explicitly set protobuf version compatible with streamlit
protobuf>=3.20,<5

# Web frameworks (Gradio for compatibility)
gradio>=4.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Optional optimizations (may not work on all platforms)
# Uncomment if deploying on GPU-enabled infrastructure
# bitsandbytes>=0.41.0
# flash-attn>=2.6.0

# Voice features (optional)
# speechbrain>=0.5.16
# torchaudio-augmentations>=0.2.4
